server:
  port: 5050


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/BUS_INFO
    username: root
    password: Pavilion@12
    driver-class-name : com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true